function Theory() {
  return (
    <div>
      <h1>Theory</h1>
      <p>
        Route-based lazy loading ensures that components associated
        with specific routes are loaded only when required.
      </p>
    </div>
  );
}

export default Theory;
